
<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Gerenciar Fotos</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <main class="container mx-auto p-4">
    <h1 class="text-2xl font-bold mb-4">Fotos</h1>
    <form action="/admin/fotos" method="POST" enctype="multipart/form-data" class="mb-6">
      <input type="text" name="legenda" placeholder="Legenda" required class="border p-2">
      <input type="file" name="postimage" accept="image/*" required class="border p-2">
      <button class="bg-green-500 text-white px-4 py-2 rounded">Adicionar Foto</button>
    </form>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
      <% fotos.forEach(foto => { %>
        <div class="bg-white shadow rounded p-4">
          <img src="<%= foto.postimagem %>" alt="foto" class="w-full h-48 object-cover mb-2">
          <p><%= foto.postlegenda %></p>
          <form action="/admin/fotos/<%= foto.postcodigo %>/excluir" method="POST" onsubmit="return confirm('Excluir esta foto?');">
            <button class="bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded mt-2">Excluir</button>
          </form>
        </div>
      <% }) %>
    </div>
  </main>
</body>
</html>