<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8" />
  <meta content="width=device-width, initial-scale=1.0" name="viewport" />
  <title><%= titulo %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Roboto', sans-serif;
    }
    .grandao {
      font-size: 4rem;
      font-weight: 700;
      color: #EAEAEA;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
      margin-bottom: 450px;
    }
  </style>
</head>
<body class="bg-black text-black">
  <div class="relative h-screen flex items-center flex-col justify-center">
    <div class="absolute inset-0 object-cover" style="background-color: #ffffff;"></div>
    <div class="grandao absolute text-black">Painel Admin</div>
    <div class="relative z-10 w-full max-w-md mx-auto p-8 rounded"
         style="background-color: #EAEAEA; box-shadow: 0 4px 20px 10px #00000040;">
      <h1 class="text-3xl font-bold mb-6">Login Administrativo</h1>
      <form action="/admin/login" method="POST" class="space-y-6">
        <div>
          <label class="block text-sm font-medium" for="email">Email</label>
          <input class="w-full p-3 border border-gray-600 bg-gray-200" id="email" name="email" type="email" required />
        </div>
        <div>
          <label class="block text-sm font-medium" for="senha">Senha</label>
          <input class="w-full p-3 border border-gray-600 bg-gray-200" id="senha" name="senha" type="password" required />
        </div>
        <% if (erro) { %>
          <div class="text-red-600 text-sm"><%= erro %></div>
        <% } %>
        <button class="w-full py-3 border border-gray-600 rounded text-black font-bold bg-gray-200" type="submit">
          Entrar
        </button>
      </form>
    </div>
  </div>
</body>
</html>
